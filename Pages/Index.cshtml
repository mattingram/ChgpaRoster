@page
@model IndexModel
@{
    ViewData["Title"] = "Member Validation";
}
    <div class="layout">
        <h2>CHGPA @ViewData["Title"]</h2>
        <p>Enter USHPA # or Email address to validate membership.</p>
        <form method="post">
            <div class="member-search">
                <table>
                    <tr>
                        <td>USHPA #:</td><td><input asp-for="Ushpa" type="number" /></td>
                    </tr>
                    <tr>
                        <td>Email:</td><td><input asp-for="Email" type="email" /></td>
                    </tr>
                </table>
                <div class="submit-button">
                    <input type="submit" value="Search" />
                </div>
                <span class="result @(@Model.IsActive ? "member-active" : "member-inactive")">@Model.Result</span>
                <span class="validation-error">@Model.Message</span>
            </div>
        </form>
    </div>